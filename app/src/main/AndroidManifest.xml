<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sqc.zcfeng.angle">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.MEDIA_CONTENT_CONTROL"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <application
        android:name=".activity.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppNoActionTheme">
        <activity
            android:name=".activity.SplashActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.MainActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activity.LoginActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".activity.HCActivity" />
        <activity android:name=".activity.NewsActivity" />
        <activity android:name=".activity.ShowNewActivity" />
        <activity android:name=".activity.DoctorAdviceListActivity" />
        <activity android:name=".activity.AdviceInfoActivity" />
        <activity android:name=".activity.IllListActivity" />
        <activity android:name=".activity.ShowIllActivity" />
        <activity android:name=".activity.ClinicActivity" />
        <activity android:name=".activity.DrugActivity" />

        <receiver
            android:name=".receiver.AlarmReceiver"
            android:process=":remote"></receiver>

        <activity android:name=".activity.AlarmActivity"
            android:theme="@style/Base.Theme.AppCompat.Light.Dialog.Alert"></activity>
    </application>

</manifest>